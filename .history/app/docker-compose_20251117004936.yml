version: '3.8'
services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./static:/app/static
    environment:
      - MISTRAL_API_KEY=${MISTRAL_API_KEY}
      - API_KEY=${API_KEY}
  # redis and worker can be added for background jobs
  # worker:
  #   build: .
  #   command: python worker.py
  #   depends_on:
  #     - redis
  # redis:
  #   image: redis:7
